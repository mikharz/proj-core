{
    "name": "proj/core",
    "type": "library",
    "autoload": {
        "psr-4": {
            "Core\\": "src/"
        }
    },
    "config": {
        "platform": {
            "php": "8.4"
        },
        "sort-packages": true
    },
    "require-dev": {
        "phpstan/phpstan": "^2.1"
    },
    "scripts": {
        "analyse": [
            "phpstan analyse --level 10 src/"
        ]
    }
}
